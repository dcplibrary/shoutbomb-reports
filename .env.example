# SMTP Server Configuration
SMTP_HOST=0.0.0.0
SMTP_PORT=2525
SMTP_SECURE=false
SMTP_REQUIRE_AUTH=false
SMTP_AUTH_OPTIONAL=true
SMTP_USERNAME=
SMTP_PASSWORD=

# Database Configuration
# Supported types: mysql, postgresql, mssql
DB_TYPE=mysql
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=email_ingester

# MSSQL Specific Options (only needed for MSSQL)
DB_ENCRYPT=true
DB_TRUST_CERT=true

# Application Configuration
DEBUG=false
MAX_EMAIL_SIZE=10485760
