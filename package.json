{
  "name": "email-ingester",
  "version": "1.0.0",
  "description": "Email ingestion service that receives emails via SMTP and stores them in a database",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["email", "smtp", "ingestion", "database"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "smtp-server": "^3.13.4",
    "mailparser": "^3.7.1",
    "dotenv": "^16.4.5",
    "mysql2": "^3.11.3",
    "pg": "^8.13.1",
    "mssql": "^11.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
